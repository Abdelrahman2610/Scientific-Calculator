package com.calculator.core;

/**
 * Enum defining supported calculator operations.
 */
public enum Operations {
    ADD, SUBTRACT, MULTIPLY, DIVIDE, MODULUS, POWER,
    SIN, COS, TAN, LOG, LN, EXP, SQUARE, RECIPROCAL, SQRT, NEGATE, FACTORIAL, PI, TEN_POWER, ABSOLUTE
}